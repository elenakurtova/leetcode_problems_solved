/**
 * 
 */
package leetcode.twosums;

/**
 * @author Elena
 *
 */
public class App {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// Testing the TwoSums method
		int nums[] = {2,4,7,8,10};
		int indices[];
		indices = TwoSums.twoSum(nums, 9);
		System.out.println("The indices of two numbers are: [" + indices[0] + "," + indices[1] + "].");
	}

}
